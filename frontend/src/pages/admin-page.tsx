import { Box, Typography } from '@mui/material';

export const AdminPage = () => {
    return (
        <Box sx={{ p: 4 }}>
            <Typography variant="h3">
                Админка
            </Typography>
        </Box>
    );
};